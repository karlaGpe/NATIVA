fun main(parametro: Array<String>) {
    // Se inicializa la variable 'x' con el valor 1
    var x = 1

    // El ciclo while se ejecuta mientras 'x' sea menor o igual a 100
    while (x <= 100) {
        // Imprime el valor actual de 'x'
        println(x)

        // Incrementa 'x' en 1 en cada iteración
        x = x + 1
    }
}
