fun main(parametro: Array<String>) {
    // El ciclo 'for' recorre los números del 1 al 100 (inclusive) y los imprime
    for (i in 1..100) {
        println(i)  // Imprime el valor de 'i' en cada iteración del ciclo
    }
}
